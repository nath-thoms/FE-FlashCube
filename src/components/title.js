import React from 'react'; // This component is not used at all currently so not necessary
import PT from 'prop-types';

function Title (props) {
    return (
        <div className="Title">
            Title
        </div>
    );
}

Title.propTypes = {
    
}

export default Title;