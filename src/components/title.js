import React from 'react';
import PT from 'prop-types';

function Title (props) {
    return (
        <div className="Title">
            Title
        </div>
    );
}

Title.propTypes = {
    
}

export default Title;